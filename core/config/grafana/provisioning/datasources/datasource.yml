# YYC³ Grafana 数据源自动配置（2026-09-24 收口：定 uid 供面板确定性引用；Loki 未部署移除）
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
