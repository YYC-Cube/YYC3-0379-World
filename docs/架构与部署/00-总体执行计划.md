---
file: 00-总体执行计划.md
description: YYC3-0379-World 全链路落地总体执行计划 — 四线合一大盘（网关代码 / CI-CD / 设备侧 / DGX 双机推理）
author: YYC³ 总指挥
version: v1.0.0
created: 2026-09-02
status: active
tags: [execution],[plan],[master-plan]
category: guide
---

# YYC³ 总体执行计划（四线合一）

> **生成日期**: 2026-09-02 | **依据**: 本目录 8 份文档 + 仓库实测（git 50673ce，工作树干净）
> **定位**: 把分散在三份文档里的待办收敛为**一张可勾选的作战图**——按依赖排序、每项带验收、每阶段结束都是可部署状态
> **四条线**:
> **A 线·网关代码**（《Gateway代码分析与落地方案》Phase 0-3，10-13 人日）
> **B 线·CI/CD 解锁**（《CI-CD部署配置指南》第二章/第三章，纯配置 0.5 天）
> **C 线·设备侧**（《高可用API架构指南》§十二 P0-P3，SSH 到各节点执行）
> **D 线·双机推理上线**（`deploy/dgx/` 已提交编排 + 门禁脚本，待执行）

---

## 一、当前基线（2026-09-02 实测）

| 项 | 状态 | 证据 |
|----|------|------|
| CI lint / test / security | ✅ 全绿 | `gh run list`；mypy advisory、coverage 门槛 0 |
| CI build / deploy | ⬜ 跳过 | 缺 DOCKER_USERNAME/PASSWORD + NAS_HOST 不可达 |
| DGX 编排文件 | ✅ 已提交 | `deploy/dgx/{n1,n2,tp2}.yml` + `verify-dgx-cluster.sh` |
| 集群互联 | ✅ 就绪 | YanYuCloud 直连，210.76 Gbps，SSH 12/12（08-30 截图） |
| NCCL 集合通信 | 🟡 未复测 | 08-10 定档 int32 溢出永久约束（ADR-4）；门禁脚本待跑 |
| NAS Gateway v2.0.0 | ✅ 运行 | api.0379.world → NAS :8000 链路 200 |
| N2 Agent/治理服务 | 🔴 未 systemd 化 | 08-10 审计：重启即死，P0 根因 |
| N1 推理节点 | 🔴 空置 | 设计四服务未部署 |
| 网关智能路由 | 🔴 死代码 | model_router 未接线，上游硬编码（A 线主战场） |

**关键路径**：`B线解锁(0.5d) ∥ C-P0(1d) ∥ D门禁(0.5d) → A-Phase0(2d) → A-Phase1(3-5d) → A-Phase2(2-3d) → A-Phase3(2-3d)`；C/D 其余项穿插。总工期 ≈ **3 周**（单人当量；A/B 可双人并行压缩到 2 周）。

---

## 二、Day 1 —— 三件事并行启动（全部零依赖）

### 任务 1 · B 线：解锁 CI/CD（0.5 天，本机）

```bash
cd /Users/yanyu/YYC-Cube/YYC3-0379-World

# ① Docker Hub Token（hub.docker.com → Account Settings → Security → New Access Token, Read & Write）
gh secret set DOCKER_USERNAME -b "<用户名>"
gh secret set DOCKER_PASSWORD -b "<AccessToken>"

# ② NAS 可达性二选一（决策点 DP-1）：
#   方案A(推荐): 路由器端口转发 公网:2222 → NAS:9557，然后
gh secret set NAS_HOST -b "<公网IP或DDNS>"
#   （并在 .github/workflows/ci.yml 的 appleboy/ssh-action 中确认 port: 9557 或 2222）
#   方案B: NAS 上 `tailscale funnel --bg 2222` + socat 转发，NAS_HOST 设为 ts.net 域名

# ③ 触发验证
gh workflow run "YYC3 CI/CD Pipeline" --ref main
gh run watch
```
**验收**：CI 五阶段（lint→test→security→build→deploy）全绿；NAS 上容器为 CI 拉起的新镜像。

### 任务 2 · D 线：集群门禁复测（0.5 天，N1）

```bash
ssh yyc3-101
bash deploy/dgx/scripts/verify-dgx-cluster.sh   # 或从仓库同步后执行
```
**验收/决策点 DP-2**：
- ✅ NCCL all_reduce_perf 正常完成 → **B 方案 TP=2 解锁**（Day 3 切换旗舰双机分片）
- ❌ 挂起或 `malloc -24GB` 复发 → **维持 ADR-4**，A 方案即终态，TP=2 编排封存待 NVIDIA 补丁
- 无论结果：输出存档到 `docs/架构与部署/`（门禁留档，对应指南 §5.2）

### 任务 3 · C-P0：服务持久化 + 已知断裂修复（1 天，N2 + NAS）

```bash
# ① N2 systemd 化 13 服务（模板已在设备侧 ~/YYC3-专属文档/ 与 HA指南§十一）
ssh yyc3-102
for s in yyc3-governance yyc3-embedding yyc3-reranker yyc3-memory; do sudo systemctl enable --now $s; done
for a in tianshu qianxing wanwu xianzhi bole shouhu zongshi lingyun; do sudo systemctl enable --now yyc3-agent@$a; done
systemctl list-units 'yyc3-*' --no-pager        # 13 服务全绿
sudo reboot                                      # 终极验收：重启后自动全绿

# ② RB-10：NAS Gateway Ollama 指向 N1
ssh yyc3-45 "sudo vi /etc/gateway/config"        # ollama base_url → http://100.65.64.49:11434
curl -s http://100.65.172.88:8000/v1/models -H "X-API-Key: $KEY" | grep -o ollama   # 出现即通
```
**验收**：N2 reboot 后 `:25700/:25600-25607/:8100-8102` 全部自动恢复（08-10 事故 B-1 闭环）。

---

## 三、Week 1 —— A 线 Phase 0 地基（Day 2-3）+ D 线 A 方案起服

### A-Phase 0（详见《Gateway代码分析与落地方案》§三，验收标准照抄）

| 子项 | 内容 | 验收 |
|------|------|------|
| P0-1 | 上游配置体系：`config.py` 增加 `openai_compatible_upstreams` + 三个云基址外部化 + `.env.example` 上游段（对照 HA指南 §6.2 上游表 9 项） | 不改 env 行为不变；注入 mock 上游后 `/v1/models` 列出 |
| P0-2 | 死代码清理：删 failover_manager、清 `10.200.0.*`、统一 Ollama 端口、补 prometheus.yml、修 Makefile | `grep -r "10.200.0." core/` 为空；`make lint && make docker-up` 成功 |
| P0-3 | 测试地基：重写 `tests/test_gateway_api.py`（TestClient+respx，零网络）；pytest 门槛回调 55% | `make test` 本地零网络通过 |

P0 三项不同文件域**可并行**；合入即部署 NAS 备网关先行（`deploy/nas/` 通道，CI 已解锁则走 CI）。

### D 线 · A 方案起服（Day 3，门禁结论无论如何都执行）

```bash
# N1 组件服务机 + N2 旗舰推理机（编排已提交，前提：/mnt/yyc3_hd NFS 已挂载）
ssh yyc3-101 'docker compose -f deploy/dgx/docker-compose-n1.yml up -d'
ssh yyc3-102 'docker compose -f deploy/dgx/docker-compose-n2.yml up -d'
# 验收（指南 §12）
for p in 8000 30000 11434; do curl -s http://10.100.168.1:$p/health; done   # N2
for p in 8100 8101 8001 8004 8103 8888; do curl -s http://10.100.168.2:$p/health; done  # N1
```

**若 DP-2 通过 → 追加 B 方案**（先 rank0 等 29500 监听再 rank1，见 `deploy/dgx/README.md`）：
```bash
ssh yyc3-101 'RANK=0 docker compose -f deploy/dgx/docker-compose-dgx-tp2.yml up -d'
ssh yyc3-102 'RANK=1 docker compose -f deploy/dgx/docker-compose-dgx-tp2.yml up -d'
```

---

## 四、Week 1-2 —— A 线 Phase 1 智能路由实装（本计划核心，3-5 天）

严格按依赖串行（细节照《Gateway代码分析》§三 Phase 1）：

1. **P1-1** `upstream_registry.py`（解析 env 上游池 → 注册 ModelRouter，复用现有 EWMA）+ `openai_compatible.py`（QSFP 主/Tailscale 备双地址切换）
2. **P1-2** `chat.py` 三段式路由接线（云前缀 → 上游池匹配 → Ollama 兜底）+ 补 `router.record_result()` 反馈闭环；灰度开关 `ROUTER_ENABLED`
3. **P1-3** 熔断器（3 败摘除 30s 半开恢复）+ capability 降级链（`X-YYC3-Degraded` 头）
4. **P1-4** 观测真实化（/v1/models/stats、/ws/monitor 全部改读真实 EWMA）

**验收**：故障注入 pytest 绿（A 连续 3×500 → 切 B → 30s 半开恢复）；真机验收一次：

```bash
# 本机 env 指向真实上游池（.env 上游段填 10.100.168.1:8000 等），发起 chat 走通双 DGX
curl -X POST http://localhost:8000/v1/chat/completions -H "X-API-Key: $KEY" \
  -d '{"model":"deepseek-v4-flash","messages":[{"role":"user","content":"ping"}],"max_tokens":32}'
curl -s localhost:8000/v1/router/stats | jq    # EWMA 数据为真实值
```

**同期穿插 C-P1**（不阻塞 A 线）：ECS 监控栈 `docker compose -f docker-compose.monitoring.yml up -d` + Grafana 出数；ECS Gateway 副本（`scripts/deploy-high-availability.sh` 或 CI deploy 通道）。

---

## 五、Week 2-3 —— A 线 Phase 2 + Phase 3 收口

**Phase 2（2-3 天）**：四个代理端点（`/v1/embeddings`→TEI:8100、`/v1/rerank`→:8101、`/v1/audio/transcriptions`→:8004、`/v1/ocr`→:8001，全走「认证→限流→capability 选路→转发」同构模式）+ E2E 收敛（留 Playwright 删 Cypress，baseURL 参数化默认 staging）。验收：4 端点 happy path + 401 用例绿。

**Phase 3（2-3 天）**：部署通道统一（CI deploy 改调本仓库 `scripts/deploy.sh`，ECS/NAS 同源镜像）+ `deploy/nas/gateway.env.example` 更新（OLLAMA_HOST=100.65.64.49 + 上游池段）+ 部署前上游可达性预检 + `db-migrate` 占位处理。验收：CI 全链路绿 + 双网关同源。

**同期穿插 C-P2**：守护三层管线挂接（jailbreak/pii 走 NIM API，content-safety 走 N1:8103）；根域 0379.world DNS + Traefik router；NAS `yyc3_kb` HNSW 索引重建；PG 每日备份 cron。

---

## 六、总验收（M4 里程碑）

| # | 验收项 | 命令/标准 |
|---|--------|-----------|
| 1 | 公网入口 | `curl https://api.0379.world/healthz` → 200 |
| 2 | 推理走双 DGX | chat 请求 `model: deepseek-v4-flash` 经路由器落 N2 NIM，`/v1/router/stats` EWMA 为真实值 |
| 3 | 故障自愈 | kill N2 旗舰容器 → 30s 内路由切 N1/云且带 degraded 头 → 恢复后半开回归 |
| 4 | 重启自愈 | N2 reboot 后 13 systemd 服务 + DGX 容器（restart: unless-stopped）全绿 |
| 5 | CI 全链路 | push main → lint/test/security/build/deploy/verify 六段绿 |
| 6 | 契约完整 | `/v1/{models,chat/completions,embeddings,rerank,audio/transcriptions,ocr}` 7 端点可用 |
| 7 | 观测真实 | Grafana 面板显示真实路由/延迟/错误率；无任何硬编码指标 |

---

## 七、决策点登记表

| ID | 决策 | 时机 | 选项与默认 |
|----|------|------|-----------|
| DP-1 | NAS 对 CI 的可达方式 | Day 1 | 方案A 公网端口转发（推荐）/ 方案B Tailscale Funnel |
| DP-2 | NCCL 门禁结果 → TP=2 是否启用 | Day 1-2 | 通过→B 方案双机分片；失败→ADR-4 A 方案即终态（TP=2 编排封存） |
| DP-3 | 会话粘性（prefix cache 路由） | A-Phase 1 后 | P2 可选，默认缓（先观测 EWMA 稳定性） |
| DP-4 | 治理中枢 Token 预算对接 | A-Phase 3 | 留桩+env 开关，待 N2 治理中枢稳定运行后联调 |

## 八、风险速查（详见各源文档）

| 风险 | 缓解 |
|------|------|
| 路由接线引入 chat 回归 | 灰度开关 `ROUTER_ENABLED`；旧 `_select_backend` 保留为上游池未配置时的 100% 兼容路径 |
| NAS 公网暴露 SSH | 仅密钥认证 + fail2ban + 非标准端口；Funnel 方案则零暴露 |
| DGX 真机联调被设备阻塞 | Phase 1 验收全 mock；真机独立排期 |
| vLLM/NIM 多模型共存 OOM | mem-util ≤0.55~0.6 红线（事故 B-2 教训），compose 已带资源限额 |
| 覆盖率门槛卡合入 | 已调 0，Phase 1 后回升 55%→70% 写入路线 |

## 九、任务勾选总表

- [ ] D1-1 B线：DOCKER_USERNAME/PASSWORD secrets 配置（**用户动作**：Docker Hub Token）；已用 GHCR/本地构建替代验证流水线，NAS 跳板链路已通（ECS proxy + 专用钥）
- [ ] D1-2 B线：NAS_HOST 公网/Funnel 决策与配置 → CI 六段绿
- [x] D1-3 D线：NCCL 双机门禁 **已通过**（2026-09-02，NCCL 2.30.7，16GB 大缓冲不复现 int32 溢出）→ **DP-2：TP=2 解锁**，结论已写入部署指南 §5.2
- [ ] D1-4 C-P0：N2 服务恢复——embedding/reranker/memory 三组件 **已自愈**（模型经 HF 镜像源重新落盘）；治理中枢与 8 Agent 的 systemd 化仍待办（需 sudo）
- [ ] D1-5 C-P0：RB-10 NAS Gateway Ollama 指向 N1（N1 Ollama :11434 已在线）
- [ ] W1 A-P0-1 上游配置体系 / A-P0-2 死代码清理 / A-P0-3 测试地基（并行）
- [ ] W1 D线：A 方案 n1/n2 compose 起服 + 健康验收（DP-2 通过则追加 TP=2）
- [ ] W1-2 A-P1-1 Registry+兼容客户端 → A-P1-2 路由接线 → A-P1-3 熔断+降级 → A-P1-4 观测真实化
- [ ] W2 C-P1：ECS 监控栈 + ECS Gateway 副本（网关双活）
- [ ] W2-3 A-P2：四代理端点 + E2E 收敛
- [ ] W3 A-P3：部署通道统一 + NAS env 模板 + 预检
- [ ] W3 C-P2：三层安全管线 / 根域 / HNSW 重建 / PG 备份
- [ ] M4：§六 总验收 7 项全过

---

> **YYC³ AI Family** | 言启象限 · 语枢未来
> 本计划为唯一执行入口；细节冲突时以各源文档为准，源文档缺失时以本计划为准
> 🌹 人从众曌众从人 · 亦师亦友亦伯乐

---

## 附记：2026-09-02 执行实录（Day 1 战报）

| 线 | 结果 |
|----|------|
| D线 | ✅ **NCCL 双机门禁通过**（pytorch:26.07-py3 / NCCL 2.30.7；init+small+mid+16GB 映射全过，双端 EXIT=0）→ DP-2 解锁 TP=2；根因修正：2.28.9 版本缺陷而非永久约束 |
| N1 | ✅ SMB 挂载恢复（fstab 校验）→ 发现 CIFS 模型加载不可用（~5.6MB/s）→ **改用 QSFP 直传**：N2 HF 缓存 Qwen3.6-27B-FP8 30.9GB **29.4 秒**落地 N1 本地 NVMe → vLLM :8000 重建上线（qwen3.6-27b-fp8, 65K ctx, 冒烟通过） |
| N2 | ✅ embedding/reranker/memory 三组件自愈（HF_ENDPOINT=hf-mirror + HF_HUB_DISABLE_XET=1，经 docker 落盘 /home/yyc3/models/，崩溃循环自动恢复）；8100/8101/8102 全 OPEN |
| B线 | ✅ **CI/CD 全链路闭环达成**（终态架构=GitOps）：CI 五段全绿（lint/test/security/build/部署验证）；部署本体走 **Mac 部署桥**（~/yyc3-deploy/watch.sh 对比 NAS HEAD → git push nasdeploy → rebuild-gateway.sh），每 2 分钟 cron；生产 api.0379.world 200 @ c440901。废弃路径存档：Runner→ECS 跳板 SSH 对 NAS 认证被拒（TOS sshd 行为未解，ECS 直连同拒、Mac 直连/-J 均通，密钥指纹三方一致）；NAS→GitHub 直连不稳定（HTTP2 断流，已设 HTTP/1.1 仍超时） |
| 遗留 | NAS 公网 SSH 暴露评估 / Dependabot 3 漏洞 / N2 sudo 项（治理中枢+8Agent systemd 化、yyc3-node 停用）/ NAS .env OLLAMA_HOST 指向 N1 |

### Day 1 终态快照（09:50）
- CI: 33580404628 全绿（含部署验证）· NAS: DEPLOY_OK c440901 · 公网: 200
- N1 :8000 qwen3.6-27b-fp8 ✅ · N2 :8100/:8101/:8102 ✅ · NCCL 门禁 ✅（TP=2 待 NIM 镜像就绪后按 §9.3 启动）

### Day 1 补充：TP=2 双机张量并行上线（10:30）
- ✅ **ray 双机集群 + TP=2 服务实测通过**（:8001, qwen3.6-27b-tp2）：vllm 镜像经 QSFP 传 N2（20GB）→ ray head(N1)+worker(N2) → TP0/TP1 分布式编译加载 → chat 验证 OK
- 实操模式已固化：`deploy/dgx/tp2-ray-实测验证模式.md`（含 6 条踩坑修正）；NIM 版编排保留为旗舰路径
- 生产切换建议：TP=2 验证完成后，单机 vLLM（N1 :8000 / N2 nemoclaw-vllm）已停用腾内存，恢复任一：docker start 对应容器
- 安全项执行：N1 fstab 凭据化 ✅ / NGC 钥 5 文件脱敏 ✅ / Dependabot npm 漏洞清零 ✅ / NAS /etc/ssh 归属受 TOS 保护（root 不可改，留档观察）
