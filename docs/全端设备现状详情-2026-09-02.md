# YYC³ 全端设备现状详情文档

> **生成时间**：2026-09-02 10:16（Asia/Shanghai）
> **生成方式**：6 台设备全量实测（Tailscale 全网状态 + ICMP ping + SSH 登录采集 + 服务端口/健康接口探测），结合 `~/YYC3-DGX-Spark/` 既有基线资料（2026-08-21 快照）
> **服务对象**：YYC3-0379-World（全链路 AI 模型网关·智能协同平台）v2.1.0 部署矩阵
> **实测执行**：yyc3-22 主控机发起

---

## 一、结论摘要

| 维度 | 结论 |
|------|------|
| 在线情况 | **6/6 台全部在线**（含 yyc3-77——其「offline 7 天」记录已过时） |
| 与清单差异 | 5 处实测结果与既有记录不符，其中 **2 项 P0 级异常** |
| 最严重问题 | `0379.world` **DNS 无解析**，公网入口实际不可达（Traefik 本身存活） |
| 次严重问题 | yyc3-33 主 Gateway :8000 **未启动**，网关双活降级为 yyc3-45 单活 |
| 进行中事项 | yyc3-101 正在部署**双机 TP2**（qwen3.6-27b-tp2），Ray placement group 等待 worker，尚未闭环 |

---

## 二、设备总览（实测快照）

| 节点 | 硬件 | Tailscale IP | 运行时长 | 资源现状 | 在线 | 关键服务实测（2026-09-02 10:16） |
|------|------|-------------|---------|---------|:----:|--------------------------------|
| **yyc3-22** | MacBook Pro M4 Max · 128GB | 100.87.159.21 | 本机 | — | ✅ | Ollama :11434 ✅（3 模型）· Agent :18789 ✅ |
| **yyc3-33** | 阿里云 ECS · 7.1GB | 100.126.132.112 | 12d 7h | 盘 11G/79G（15%） | ✅ | Traefik :80/:443 ✅ · Redis :6379 ✅ · PG16 :**5433** ✅ · Gateway :8000 ❌ · 0379.world DNS ❌ |
| **yyc3-45** | TerraMaster F4-423 · N5095/32GB | 100.65.172.88 | 4d 17h | /Volume1 8.2T/14T（63%） | ✅ | Gateway v2.0.0 :8000 ✅ healthy · Redis/PG healthy（仅本机绑定） |
| **yyc3-101** | DGX Spark GB10 (N1) · 121GB UMA | 100.65.64.49 | 3d 6h | 内存 6.3/121G · 盘 19% | ✅ | vLLM :8000 ❌ · Ollama :11434 ❌ · **tp2-head 启动中 ⚠️**（Ray 等 worker） |
| **yyc3-102** | DGX Spark GB10 (N2) · 121GB UMA | 100.76.167.103 | 3d 6h | 内存 95/121G · 盘 17% | ✅ | vLLM :8000 ✅（Qwen3.6-27B-FP8, 64K ctx）· Embedding :8100 ✅ · 治理 :25700 ❌ · Agent :25600 ❌ |
| **yyc3-77** | iMac M4 · 32GB | 100.98.206.18 | — | — | ✅ | ping/TS ✅ 在线 · **SSH ❌ publickey denied** |

> 网络质量（ICMP 实测）：101 13ms · 102 16ms · 33 25ms · 77 374ms（高延迟，iMac 疑似休眠唤醒或 Wi-Fi）· 45 684ms（NAS 中继链路 relay "nue"，非直连）。
> 另有 3 个 Tailscale 节点 offline 未在清单内：yyc3-22-2（22d）、yyc3-88-1（12d）、yyc3-99-1（24d）。

---

## 三、逐台设备详情

### 3.1 yyc3-22 · 主控 / 开发中枢 ✅

- **角色**：主控机、开发环境、SSH 枢纽、MLX 微调、本机推理兜底
- **系统**：macOS（M4 Max / 128GB / 2TB+2TB + 外置盘）
- **Tailscale**：100.87.159.21（yyc3-22-1，offers exit node）
- **服务实测**：
  - Ollama :11434 ✅ — 已加载模型：`yyc3-family-coder:14b-q4`（9.0G，家族编码主模型）、`qwen3-coder-30b`（18.6G）、`nemotron-3.5-content-safety`
  - Agent 服务 :18789 ✅ 端口开放
- **与基线差异**：无。与 08-21 记录一致。

### 3.2 yyc3-33 · 阿里云 ECS · 公网入口 / 生产网关 ⚠️ 部分异常

- **角色**：生产流量入口（Traefik SSL 终结 + rate-limit + fail2ban）、Gateway 主节点、PG 生产主库、Redis
- **系统**：Linux ECS，7.1GB 内存，已运行 12 天 7 小时，load ≈ 0
- **Tailscale**：100.126.132.112（公网 IP 39.97.53.176，TS 直连）
- **部署目录**：`/root/0379-world/`（app / certs / config / data / docker / docker-compose.traefik.yml 等）
- **服务实测**：

| 服务 | 期望端口 | 实测 | 说明 |
|------|---------|------|------|
| Traefik v3.0 | 80 / 443 | ✅ Up 12d | compose 项目 `docker` running(1)；IP 直连 :80 返回 301（正常跳 HTTPS） |
| Redis | 6379 | ✅ | redis-server 原生进程监听 0.0.0.0 |
| PostgreSQL 16 | 5432 | ✅ 实为 **5433** | systemd `postgresql@16-main` active；监听 0.0.0.0:**5433**。旧文档写 5432 属记录偏差 |
| Gateway | 8000 | ❌ **未监听** | compose 仅运行 Traefik 一个容器；主 Gateway 进程未启动 |
| 0379.world | DNS | ❌ **无解析** | `nslookup 0379.world → No answer`。DNS 记录失效/被删，公网用户无法到达 |

- **结论**：Traefik、Redis、PG、证书目录均恢复（与「已恢复」记录一致），但**公网链路整体不可用**——DNS 失效 + 主 Gateway 未启动。用户从公网访问 `0379.world` 会直接失败。

### 3.3 yyc3-45 · TerraMaster NAS · 存储中心 / 网关备 ✅

- **角色**：模型仓库（NFS）、Gateway 备节点、Redis 备、PG14
- **系统**：TerraMaster F4-423（N5095 4 核 / 32GB），hostname `YanYuCloudCube`，运行 4 天 17 小时
- **Tailscale**：100.65.172.88:9557（SSH），当前走 relay "nue" 中继（延迟 684ms，非直连）
- **服务实测**：

| 服务 | 实测 | 说明 |
|------|------|------|
| Gateway v2.0.0 :8000 | ✅ healthy | `uptime_seconds=2593`（**约 43 分钟前重启过**，原因未查）；redis/postgresql 子服务 healthy |
| Ollama | unreachable | 45 上未部署 Ollama（Ollama 在 22/101），Gateway 报 unreachable 属预期，非故障 |
| Redis :6399 | 仅本机 | 外部 nc 不通，Gateway 本机连接正常——绑定 localhost 的安全配置，非故障 |
| 存储 | 63% | `/Volume1` 14T 已用 8.2T，**剩余 5.0T**；模型仓库（DeepSeek-V4 / GLM-5.1 / Kimi-K2.6 等） |
| docker ps | 无权限 | SSH 用户无 docker 组权限，未影响核心结论 |

### 3.4 yyc3-101 · DGX Spark GB10 (N1) · 推理主节点 ⚠️ 部署进行中

- **角色**：vLLM 主推理（基线：qwen3.6:35b-a3b MoE）、Ollama 管理/编码模型
- **系统**：DGX Spark GB10，121GB UMA / 3.7TB NVMe，运行 3 天 6 小时
- **Tailscale**：100.65.64.49（TS 直连 42.224.187.43:14180）；QSFP 双机互联 10.100.168.2/169.2
- **服务实测**：
  - vLLM :8000 ❌ 无响应；Ollama :11434 ❌ 未运行
  - **内存仅用 6.3G/121G**——印证「模型未常驻」，与 08-21 基线（110/121G，满载推理）差异巨大
  - **进行中**：容器 `tp2-head` 于 10:15 启动（实测时 Up <2 min），进程为
    `vllm serve qwen3.6-27b-tp2 --tensor-parallel-size 2 --distributed-executor-backend ray --max-model-len 32768 --gpu-memory-utilization 0.35 --kv-cache-dtype fp8 --port 8001`
    Ray 连接 `192.168.3.101:6379` 集群成功，但**卡在等待 placement group（GPU 1.0 × 2）**——`tp2-worker`（102 侧）未上线。102 上 `nemoclaw-vllm` 占用 GPU 资源，疑似 worker 无法调度的根因。
  - 另有 `openshell-openclaw-local` 容器 Up 3 days (healthy)
- **结论**：101 当前**没有可用的推理服务**（旧 35B 已停，新 TP2 未闭环），正在向「双机 TP2 27B」架构迁移的中途。

### 3.5 yyc3-102 · DGX Spark GB10 (N2) · 训练 / 推理节点 ✅（部分服务缺失）

- **角色**：vLLM 推理、8 Agent 集群（:25600-25607）、治理中枢（:25700）、Embedding/Reranker/Memory
- **系统**：DGX Spark GB10，121GB UMA / 3.7TB NVMe，运行 3 天 6 小时
- **Tailscale**：100.76.167.103（TS 直连 42.224.187.43:35728）；QSFP 10.100.168.1/169.1
- **服务实测**：

| 服务 | 实测 | 说明 |
|------|------|------|
| vLLM :8000（nemoclaw-vllm 容器） | ✅ Up 3d | 模型 `Qwen/Qwen3.6-27B-FP8`，`max_model_len=65536`（基线记录 256K ctx，当前配置为 64K） |
| Embedding :8100 | ✅ 开放 | — |
| 治理中枢 :25700 | ❌ 关闭 | 08-21 基线为运行中，当前未启动 |
| Agent 集群 :25600-25607 | ❌ 关闭 | 同上；系统内 agent 相关进程仅 2（非服务进程） |
| 内存 | 95G/121G | 高占用，主要被 vLLM 及常驻任务消耗 |

### 3.6 yyc3-77 · iMac M4 · 热备 / 测试环境 ✅ 在线（记录需修正）

- **角色**：测试环境、PG 副本（:5434）、第三数据副本
- **Tailscale**：100.98.206.18（yyc3-77-1，macOS，offers exit node）——状态 **idle（在线）**
- **实测**：
  - ICMP ping ✅（374ms，延迟偏高，疑似 Wi-Fi 或休眠唤醒后未满速）
  - SSH ❌ `Permission denied (publickey,...)`——本机 `id_ed25519` 未加入该机 `my` 用户授权，**热备当前处于「在线但不可管理」状态**
- **结论**：「offline 7 天未上线」记录已过时，设备已回网；但管理通道未打通，第三副本/PG 副本状态无法远程确认。

---

## 四、实测与既有记录的差异清单

| # | 项 | 记录（清单/08-21 基线） | 实测（09-02 10:16） | 级别 |
|---|----|----------------------|--------------------|:----:|
| 1 | 0379.world 公网入口 | ✅ 已恢复（Traefik+Redis+证书） | **DNS 无解析**，公网完全不可达；Traefik/IP 直连本身正常 | 🔴 P0 |
| 2 | yyc3-33 主 Gateway :8000 | ✅ 双活 | **未启动**，仅 Traefik 容器在跑；双活降级为 45 单活 | 🔴 P0 |
| 3 | yyc3-77 热备 | 🔴 offline 7 天 | **在线**（TS idle + ping 通），但 SSH 密钥未授权 | 🟡 修正+P2 |
| 4 | yyc3-101 推理 | ✅ vLLM 运行中（35B，内存 110G） | vLLM :8000 无服务，**正在部署双机 TP2**（qwen3.6-27b-tp2），Ray 等 worker 卡住 | 🟡 P1 |
| 5 | yyc3-102 治理+Agent | ✅ 治理 :25700 + 8 Agent | **均未运行**，仅 vLLM 27B + Embedding :8100 存活 | 🟡 P1 |
| 6 | yyc3-45 Gateway | 稳定运行 | 健康，但 **43 分钟前发生过重启**（原因未查） | ⚪ P2 |
| 7 | yyc3-33 PG 端口 | 5432 | 实际监听 **5433**（文档记录偏差） | ⚪ 记录修正 |
| 8 | yyc3-102 vLLM 上下文 | 256K ctx | 当前配置 **64K** | ⚪ 记录修正 |

---

## 五、风险与建议行动（结合 0379-World 项目所需）

**P0（今日处理）**

1. **修复 0379.world DNS 解析**：到域名 DNS 服务商处恢复 A 记录 → 39.97.53.176。这是全部公网流量的前提，Traefik/证书/后端都在等这一步。
2. **启动 yyc3-33 主 Gateway**：`/root/0379-world` 部署目录完整，补拉 Gateway 服务（compose 或 systemd）并验证 `:8000/health`，恢复 33主+45备 双活。

**P1（本周内）**

3. **闭环 101 双机 TP2**：决策 GPU 分配——要么停掉 102 的 `nemoclaw-vllm` 让 `tp2-worker` 加入 Ray 集群，要么放弃 TP2 改回 101 单机推理；当前「两台各占一半」的状态使新旧两套推理互锁。
4. **恢复 102 治理中枢 + Agent 集群**（:25700 / :25600-25607）：0379-World 的智能协同层依赖它们，当前缺失。
5. **打通 yyc3-77 SSH**：将 yyc3-22 的 `id_ed25519` 公钥加入 77 的 `my` 用户，恢复第三副本/PG 副本的可管理性。

**P2（例行关注）**

6. 排查 yyc3-45 Gateway 今晨重启原因（看容器/系统日志，确认非 OOM 或崩溃循环）。
7. 更新网络拓扑与设备文档中的记录偏差（PG 5433、102 ctx 64K、77 在线状态）。
8. 三个游离 TS 节点（22-2 / 88 / 99）长期 offline，确认是否退役或回收 Tailscale 授权。

---

## 附录：实测方法与原始证据

| 步骤 | 命令/方法 | 时间 |
|------|----------|------|
| 全网节点状态 | `tailscale status` | 10:14 |
| 连通性 | `ping -c 2` 全部 5 个 TS IP | 10:15 |
| 系统采集 | `ssh <alias> 'uptime; free; df; docker ps'`（33/45/101/102 成功，77 密钥拒绝） | 10:16 |
| 服务探测 | `curl /health`、`curl /v1/models`、`nc -z`（8000/6379/6399/5432/25700/8100/25600/11434/18789） | 10:16-10:18 |
| 深挖 | `ss -tlnp`、`systemctl list-units`、`docker compose ls`、`docker logs tp2-head`、`nslookup 0379.world` | 10:17-10:20 |

基线参考：`~/YYC3-DGX-Spark/YYC3-设备-模型全量信息文档.md`（08-21）、`YYC3-高可用多设备网络拓扑-可视化架构链路文档.md`、`YYC3-33-ECS上线-运维总结与后续执行方案.md`。
