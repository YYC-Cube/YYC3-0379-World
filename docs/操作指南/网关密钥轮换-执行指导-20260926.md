---
file: 网关密钥轮换-执行指导-20260926.md
description: api.0379.world 全链路密钥轮换执行指导——P0-S1 泄露事件闭环（背景分析 + 资产盘点 + 分步 runbook + 验证矩阵 + 回滚预案 + 周期续期机制）
author: Intelligent Application Implementation Expert <admin@0379.email>
version: v1.0.0
created: 2026-09-26
updated: 2026-09-26
status: active
tags: [ops],[security],[key-rotation],[gateway],[runbook]
category: ops-guide
language: zh-CN
changelog:
  - { version: v1.0.0, date: 2026-09-26, author: 智能应用落地专家, change: 初版——P0-S1 泄露事件触发的强制轮换执行指导（含拓扑修正分析与双网关 runbook） }
related:
  - "[API认证使用指南.md](./API认证使用指南.md)（ADMIN_API_KEYS 语义契约与 rotate_admin_key.sh）"
  - "[../架构与部署/API全链路闭环文档.md](../架构与部署/API全链路闭环文档.md)（ECS Traefik → NAS:8000 拓扑）"
  - "YYC3-MiniMax-H3/docs/YYC3-MiniMax-H3-生产闭环 07/09/11 篇（H3 面板 Nginx / 鉴权 / 90 天轮换机制）"
---

# 网关密钥轮换 · 执行指导（P0-S1 事件闭环）

> **一句话**：两枚 API Key 曾以明文出现在仓库文档中（已按泄露处理），本文档给出新键替换的完整执行路径——先修正落点认知，再分阶段零中断轮换，最后建立 90 天周期续期机制。

---

## 一、背景与事件总结

### 1.1 事件时间线

| 时间 | 事件 |
| ---- | ---- |
| 2026-09-25 | H3 生产闭环蓝图 16 篇入库；[09-公网访问的鉴权配置](../../../YYC3-MiniMax-H3/docs/YYC3-MiniMax-H3-生产闭环/09-公网访问的鉴权配置.md) 中 `API_KEY_ADMIN` / `API_KEY_READONLY` **两枚密钥以明文形式写入文档**（P0-S1 发现） |
| 2026-09-25 | 文档完成占位符化脱敏，密钥真值迁出至 `.secrets/`（gitignore 隔离，`chmod 600`）；**历史提交中仍留存旧键** → 按「已泄露」处理 |
| 2026-09-26 | 新键组已生成：`.secrets/gateway_api_keys_rotated_20260926.env`（600 权限）——本文档指导将其部署生效并吊销旧键 |

### 1.2 处置原则（为什么必须轮换而非「改文档」）

1. **git 历史不可篡改假设**：旧键存在于历史提交，任何拿到仓库读权限（含 fork/clone/CI 缓存）的主体均可回溯提取；
2. **泄露面等价原则**：无法证明「没人看过」→ 一律按已泄露处理（与 NIST SP 800-57 密钥生命周期纪律一致）；
3. **最小窗口原则**：轮换存在双键过渡窗口（§四），过渡期越长风险敞口越大——建议 **24 小时内完成收敛**。

---

## 二、资产与影响面分析

### 2.1 真实拓扑（分析修正）

> ⚠️ **重要认知修正**：此前表述「SSH 至 ECS 替换 Traefik/Nginx 的 ADMIN_API_KEYS」**不准确**。经 [API全链路闭环文档](../架构与部署/API全链路闭环文档.md) 与 [API认证使用指南](./API认证使用指南.md) 核实，API Key 并不存在于 Traefik/Nginx 配置中，而是存放于**网关应用层 `.env`**：

```
公网客户端
    ↓ https
yyc3-33 ECS：Traefik 边缘反代（api.0379.world）
    │   职责：TLS 终结 / rate-limit / fail2ban / 安全头 —— ❌ 无任何 API Key
    ↓ 反代
yyc3-45 NAS：0379-World 网关（FastAPI，NAS:8000，公网唯一计算实例）
    │   ✅ 密钥真实落点：/volume1/docker/yyc3-gateway/.env
    │      ├─ API_KEYS        （业务/推理面，X-API-Key 头）
    │      └─ ADMIN_API_KEYS  （管理面 /v1/admin/** + H3 runner 密钥同源）
    ↓ 静态代理
Nginx（/h3-dashboard/ 面板静态站点）
        职责：静态文件 + 路径代理 —— H3 面板键（READONLY）的校验层之一 ⚠️ 落点待现场确认
```

### 2.2 需换键的落点清单

| # | 落点 | 变量/机制 | 是否需替换 | 依据 |
| - | ---- | --------- | ---------- | ---- |
| L1 | NAS 网关 `.env`（`/volume1/docker/yyc3-gateway/.env`，路径待现场确认） | `ADMIN_API_KEYS`（含 H3 `H3_VIDEO_RUNNER_KEY` 同源键） | **是** | 管理面键；runner 依赖它 claim 任务 |
| L2 | NAS 网关 `.env` | `API_KEYS`（业务键） | 视情况 | 若业务键与泄露键同值则换；无泄露记录则可暂缓（标注待确认） |
| L3 | H3 面板公网入口（doc 09 鉴权层，Nginx 或网关中间件） | `API_KEY_ADMIN` / `API_KEY_READONLY` | **是** | 泄露键的直接宿主 |
| L4 | ECS Traefik | — | 否（无键） | 仅边缘反代；确认无 `X-API-Key` 类静态配置残留即可 |
| L5 | H3 Agent 网关 claim（`AGENT_CLAIM_SECRET`） | HMAC 共享密钥 | 否（独立密钥，未泄露） | 2026-09-26 新生成，与本次轮换无耦合 |

### 2.3 依赖旧键的客户端清单（切换通知面）

| 客户端 | 使用的键 | 切换动作 |
| ------ | -------- | -------- |
| H3 `video_task_runner`（夜间批量） | `H3_VIDEO_RUNNER_KEY`（ADMIN 组） | L1 收敛前更新 Mac 侧 `.secrets` 环境注入 |
| 可视化面板（公网 fetch） | `API_KEY_READONLY` | 面板请求头/数据源配置更新 |
| 外部管理脚本（virtual-keys 管理等） | ADMIN 键 | 分发新键并更新本地保管 |
| 漫剧侧 H3VisionClient | `AGENT_CLAIM_SECRET` | **不在本次范围**（独立密钥体系） |

---

## 三、新键资产与保管纪律

```bash
# 新键唯一保管位置（Mac 工作机，已 600，gitignore 隔离，不入任何仓库）
/Users/yanyu/YYC-Cube/YYC3-MiniMax-H3/.secrets/gateway_api_keys_rotated_20260926.env
#   API_KEY_ADMIN=yyc3_…      ← L1/L3 管理面新键
#   API_KEY_READONLY=yyc3_…   ← L3 只读新键
```

**纪律**：

1. 新键只经「终端短暂展示 → 立即写入目标 `.env`」路径流转，**禁止**写入聊天记录、issue、git 跟踪文件；
2. 传输通道仅限：SSH 直连（scp/直接 echo）或密码管理器；禁止经第三方 IM/邮件明文传递；
3. 旧键吊销验证完成前，`.secrets/gateway_api_keys.env`（旧键）**保留不删**（回滚依赖），验证完成后归档标记 `[RETIRED-20260926]`。

---

## 四、执行指导（五阶段零中断轮换）

> 执行身份：Mac 工作机（yanyu）+ SSH 至 yyc3-45 NAS（网关宿主）。ECS 仅在 §4.4 巡检时短暂登录。
> 时间预算：操作约 30 分钟 + 过渡窗口（客户端切换）建议 ≤24h。

### Phase 0 准备与备份（NAS 上）

```bash
# 0.1 登录 NAS 并定位网关 compose 目录（ENV_FILE 按实际路径调整，通常为 gateway compose 同目录）
ENV_FILE="/volume1/docker/yyc3-gateway/.env"      # ⚠️ 待现场确认：find /volume1/docker -name '.env' -path '*gateway*'

# 0.2 确认现状（只看键名与键数量，不回显键值）
grep -c '^ADMIN_API_KEYS=' "$ENV_FILE"            # 期望 1
grep -oE '^[A-Z_]+=' "$ENV_FILE" | sort           # 变量清单（确认 API_KEYS/ADMIN_API_KEYS 存在）

# 0.3 双备份（带时间戳；sed 的 .bak 不够可靠，显式备份）
cp -p "$ENV_FILE" "$ENV_FILE.bak-rotate-$(date +%Y%m%d-%H%M%S)"
```

### Phase 1 阶段①双写（旧,新 并存——零中断窗口开启）

优先使用内置脚本（双写守卫 + 自动备份 + 预检）：

```bash
# 在仓库工作区（Mac 或 NAS）执行；脚本路径以部署实际为准
bash core/scripts/rotate_admin_key.sh status     # 先看当前态
bash core/scripts/rotate_admin_key.sh stage1     # ADMIN_API_KEYS=旧KEY,新KEY 并存 → 重启网关
```

脚本不可用时的手工等价：

```bash
NEW_ADMIN="yyc3_<从 rotation env 文件读取 API_KEY_ADMIN 值>"   # 不要写进 shell 历史可用 read -s
read -s -p "Paste NEW ADMIN KEY: " NEW_ADMIN && echo

if grep -q '^ADMIN_API_KEYS=' "$ENV_FILE"; then
    sed -i.bak "s|^ADMIN_API_KEYS=.*|ADMIN_API_KEYS=旧KEY,${NEW_ADMIN}|" "$ENV_FILE"
fi
grep -c '^ADMIN_API_KEYS=' "$ENV_FILE"           # 期望仍为 1（防重复行）
grep -o '^ADMIN_API_KEYS=' "$ENV_FILE"           # 不回显值，仅确认行存在

# 立即双验（旧新两枚都应 200）
curl -s -o /dev/null -w 'old:%{http_code}\n' -H "X-API-Key: <旧ADMIN键>" https://api.0379.world/v1/admin/virtual-keys
curl -s -o /dev/null -w 'new:%{http_code}\n' -H "X-API-Key: ${NEW_ADMIN}" https://api.0379.world/v1/admin/virtual-keys
```

> 若网关容器名非默认，`docker restart` 目标以 `docker ps | grep -i gateway` 实测为准（文档中出现过 `yyc3-gateway` 与 `0379-world-gateway-1` 两个名字，⚠️ 待现场确认）。

### Phase 2 客户端切换（过渡窗口内完成）

```bash
# 2.1 Mac 侧更新本地保管（H3 仓 .secrets 为主保管，gitignore 隔离）
#     将 gateway_api_keys_rotated_20260926.env 中的键分发至各客户端环境注入点

# 2.2 H3 video_task_runner（夜间批量依赖）：
#     更新 Mac 注入 H3_VIDEO_RUNNER_KEY 的环境文件为新 ADMIN 组键值

# 2.3 可视化面板只读键：更新面板数据源请求头（注意：doc 09 已弃用 URL 参数传键，
#     一律 X-API-Key 头），并触发一次面板刷新验证

# 2.4 外部管理脚本：逐一更新并抽测一条 admin 只读调用
```

### Phase 3 阶段②收敛（旧键即刻吊销）

```bash
# 脚本路径（预检不过不修改，安全收敛）
bash core/scripts/rotate_admin_key.sh stage2     # ADMIN_API_KEYS 只留新键 → 重启网关

# 手工等价：
sed -i.bak "s|^ADMIN_API_KEYS=.*|ADMIN_API_KEYS=${NEW_ADMIN}|" "$ENV_FILE"

# L3 同步收敛：H3 面板鉴权层（网关中间件或 Nginx auth 配置，落点以 Phase 1 的 grep 定位为准）
#   API_KEY_ADMIN / API_KEY_READONLY 全部替换为新键并重载：
docker restart <网关容器>                          # 网关层
nginx -s reload                                    # 若 Nginx 层持有键（⚠️ 待现场确认是否存在）
```

### Phase 4 巡检与吊销验证（三连验证 + 旧键死亡确认）

```bash
# 4.1 三连验证（对齐 verify_admin_keys.sh 语义）
bash core/scripts/verify_admin_keys.sh <新业务KEY> <新ADMIN键> https://api.0379.world
#   ① 业务键 → /v1/admin/*  期望 403
#   ② 管理键 → /v1/admin/*  期望 200
#   ③ 业务键 → /v1/models   期望 200（推理面无感）

# 4.2 旧键死亡确认（两枚泄露旧键都必须 401/403）
curl -s -o /dev/null -w 'old-admin:%{http_code}\n'    -H "X-API-Key: <旧ADMIN键>"    https://api.0379.world/v1/admin/virtual-keys
curl -s -o /dev/null -w 'old-readonly:%{http_code}\n' -H "X-API-Key: <旧READONLY键>" https://api.0379.world/v1/models

# 4.3 H3 业务链路回归：夜间批量前手动跑一次 runner 领取（--once）确认新键生效

# 4.4 ECS Traefik 巡检（确认无键残留，无需重载）
ssh <yyc3-33> 'grep -rE "X-API-Key|API_KEY" /etc/traefik/ 2>/dev/null; echo done'
```

---

## 五、验证矩阵（执行完成判据）

| # | 检查项 | 命令/方式 | 期望 |
| - | ------ | --------- | ---- |
| V1 | 新 ADMIN 键进管理面 | curl `/v1/admin/virtual-keys` | 200 |
| V2 | 业务键被管理面拒绝 | curl 同上（业务键） | 403 |
| V3 | 推理面不受影响 | curl `/v1/models`（业务键） | 200 |
| V4 | 旧 ADMIN 键已死 | curl 同 V1（旧键） | 401/403 |
| V5 | 旧 READONLY 键已死 | curl 同 V3（旧键） | 401/403 |
| V6 | H3 runner 领取正常 | `video_task_runner --once`（或 --dry-run） | 任务领取/心跳 200 |
| V7 | 面板公网加载正常 | 浏览器打开 dashboard + DevTools 网络面板 | batches.json 200 |
| V8 | `.env` 健康度 | `grep -c '^ADMIN_API_KEYS='` | 恰为 1 |
| V9 | 无键值入库 | 两仓 `git log -p | grep yyc3_` / grep 明文键 | 0 命中 |

**V1–V9 全绿方视为闭环完成。**

---

## 六、回滚预案

| 场景 | 动作 |
| ---- | ---- |
| Phase 1 后新键验证失败 | `bash core/scripts/rotate_admin_key.sh rollback`（恢复最近 `.env.bak-rotate-*` 并重启）——旧键仍在双写态，业务无感 |
| Phase 3 后发现客户端漏切换 | 临时回双写态：`sed` 将旧键追加回 `ADMIN_API_KEYS=旧,新` → 重启 → 补切换 → 再收敛 |
| 网关重启失败 | 按 [API认证使用指南](./API认证使用指南.md) 的手动命令指引；NAS 侧 `docker logs` 排障 |
| 全链路不可用（极端） | 恢复 Phase 0 的完整备份 `$ENV_FILE.bak-rotate-<时间戳>` 并重启网关；此时回到「泄露键仍有效」状态，须重新走 §四 |

---

## 七、周期续期机制（90 天强制轮换 + 7 天过渡）

对齐 [11-密钥自动轮换脚本](../../../YYC3-MiniMax-H3/docs/YYC3-MiniMax-H3-生产闭环/11-密钥自动轮换脚本.md) 蓝图与 `rotate_admin_key.sh` 现网机制：

| 参数 | 值 | 说明 |
| ---- | -- | ---- |
| 轮换周期 | **90 天** | ADMIN 与 READONLY **独立排期**（错峰 45 天，避免同时轮换的排障叠加） |
| 过渡期 | **7 天** | 双写并存，客户端陆续切换；第 7 天强制收敛 |
| 备份保留 | ≥3 个周期 | `.env.bak-rotate-*` / `key-backup/` |
| 审计 | 每次轮换留痕 | 执行人/时间/验证矩阵结果，记录至 NAS `/data/ops/key-backup/audit.log`（路径对齐 doc 11） |

**落地建议（本次轮换即首期锚点）**：

```bash
# NAS crontab（⚠️ rotate 脚本部署路径待现场确认后填入；先以 calendar 提醒人工执行过渡）
# 0 3 1 */3 *  bash /data/ops/rotate_api_key.py --key admin   >> /data/ops/rotate.log 2>&1
# 0 3 1 */3 *  bash /data/ops/rotate_api_key.py --key readonly >> /data/ops/rotate.log 2>&1
#   （doc 11 蓝图部署位为 /data/ops/rotate_api_key.py；若实际落在 NAS yyc3-45，须同步修正路径）
```

**轮换纪律清单（每次执行）**：

- [ ] 新键经 `secrets.token_urlsafe` 系生成，禁手工造键
- [ ] 双写 → 验证 200/200 → 客户端切换 → 收敛 → 旧键 401 确认（顺序不可颠倒）
- [ ] 全程零明文落盘（聊天/工单/git）
- [ ] 轮换完成后在 `.secrets/` 归档标记 `[RETIRED-<日期>]`

---

## 八、本次执行速查卡（TL;DR）

```bash
# ① NAS：定位 + 备份
ENV_FILE=/volume1/docker/yyc3-gateway/.env; cp -p "$ENV_FILE" "$ENV_FILE.bak-rotate-$(date +%Y%m%d-%H%M%S)"
# ② 双写（脚本优先）
bash core/scripts/rotate_admin_key.sh stage1
# ③ 切客户端（runner 键 / 面板只读键 / 管理脚本）
# ④ 收敛 + 三连验证 + 旧键死亡确认
bash core/scripts/rotate_admin_key.sh stage2 && bash core/scripts/verify_admin_keys.sh <业务KEY> <新ADMIN键> https://api.0379.world
# ⑤ 记录审计 + .secrets 归档标记 [RETIRED-20260926]
```

---

## 九、Phase 0 勘察实证（2026-09-26 执行 · 只读，未做任何变更）

> 本节为实际 SSH 勘察结果，**修正本文档 §二 的拓扑推测**，并**改变处置建议**。

### 9.1 真实拓扑（实证）

| 层 | 实证 | 勘误 |
| -- | ---- | ---- |
| ECS yyc3-33 | 仅 `docker-traefik-1`（80/443）；配置 `/root/0379-world/traefik/{traefik,dynamic}.yml`；`api.0379.world → http://100.65.172.88:8000` | ✅ 无密钥，此前判断正确 |
| NAS yyc3-45 | 容器 `0379-world-gateway-1`（uvicorn app.main:app，bridge 模式经 docker-proxy 发布 8000）；**无独立 Nginx 容器**（面板由网关一体服务） | ⚠️ 修正：「Nginx 层持键」不存在，L3 收敛点即网关 `.env` |
| **ENV_FILE** | **`/Volume2/yyc3-33/.env`**（600，属主 YYC3） | ⚠️ 修正：非 `/volume1/docker/...`，NAS 数据卷为 `/Volume1/2/3` 大写 |
| 容器管理 | NAS **无 docker CLI**，经 `/var/run/docker.sock` API 操作；compose 工程 `/Volume2/yyc3-33/deploy/nas/docker-compose.nas.yml` | 重启：`curl --unix-socket /var/run/docker.sock -X POST http://localhost/containers/<id>/restart` |
| 部署桥 | NAS `auto-deploy.sh` 监听 git 新提交 → `compose up -d --build gateway`，**不同步 .env**（直接改 .env 安全） | 方式 B 手工重启可用 sock API 等价替代 |
| 变量实测 | `ADMIN_API_KEYS`：1 键 ×41 字符（符合 `sk-admin-<32hex>` 生成器）；`API_KEYS`：1 键 ×**20 字符（偏短）** | — |

### 9.2 关键结论：泄露键 ≠ 生产真实键

逐值哈希比对（仅输出 verdict，不回显）：

| 比对 | 结果 |
| -- | ---- |
| doc09 泄露 `API_KEY_ADMIN`（26 字符） vs 现网 `ADMIN_API_KEYS`（41 字符） | **不匹配** |
| doc09 泄露 `API_KEY_READONLY` vs 现网 `API_KEYS` | **不匹配** |

**判定**：doc 09 蓝图收录的是**示例/占位键值**，从未是生产真实密钥——**生产密钥面从未暴露，紧急轮换的必要性解除**（P0-S1 从「泄露事件」降级为「文档卫生事件」，文档脱敏已完成即闭环）。

### 9.3 处置建议变更（替代 §四 紧急轮换）

1. **不执行**紧急 dual-write/stage1-2（避免无收益的生产变更风险）；
2. **排期常规 90 天轮换**（§七 机制不变），首期建议即轮换 `API_KEYS`（20 字符偏短，熵余量不足；`ADMIN_API_KEYS` 41 字符符合生成器规范，随首期同步轮换亦可）；~~待排期~~ **✅ 已于 2026-09-26 执行 stage1（见 §十）**；
3. `AGENT_CLAIM_SECRET`（H3 Agent 网关）为 2026-09-26 新生成的独立 HMAC 密钥，与本次无关、无需处置。

### 9.4 附带观察（非本任务范围）

- `auto-deploy.log` 记录 **2026-09-24 22:56 SMOKE_FAIL**（5 项冒烟全挂）；2026-09-26 复核 `/health`=200、`/v1/models`（无凭证）=401（设计行为）、公网入口 401——**当前服务健康**，09-24 为部署期瞬断，建议后续排查当时构建是否引入回归（可另立项）。

---

**文档状态**：✅ Phase 0 已完成并实证；紧急轮换**取消**（§九.9.3），常规轮换排期待排期
**执行人**：智能应用落地专家　**勘察日期**：2026-09-26　**验证矩阵结果**：N/A（未变更生产，V1-V9 留待常规轮换时执行）

---

## 十、执行记录：API_KEYS 常规轮换 stage1（2026-09-26 已执行 ✅）

### 10.1 变更内容

- `API_KEYS` 双写：`旧键(20字符),新键(sk-+48hex)`；旧键全程有效（零中断），新键已生效
- 备份：`/Volume2/yyc3-33/.env.bak-20260926-rot`（cp -p 保留 600）
- 新键保管：Mac `.secrets/api_keys_live_20260926.env`（600）

### 10.2 验证矩阵实跑结果

| 项 | 结果 |
| -- | ---- |
| V1 新键本机 `/v1/models` | **200** ✅ |
| V4 旧键本机 `/v1/models`（兼容期） | **200** ✅ |
| V6 新键公网 `api.0379.world/v1/models` | **200** ✅ |
| V7 伪造键 | 403 ✅ |
| V8 无凭证 | 401 ✅ |
| /health（compose 重建后第 3 次轮询） | 200 ✅ |

### 10.3 两条血泪修正（修正 §四/§六 runbook，后续轮换必读）

1. **`docker restart` 不重读 `.env`**——compose `environment: API_KEYS=${API_KEYS}` 在**容器创建时**完成插值；改 `.env` 后必须 `compose up -d --no-build --force-recreate gateway`（docker CLI 实证路径：`/Volume3/@apps/DockerEngine/dockerd/bin/docker`）。此前两次验证失败（新键 401/403）皆因此因，非密钥问题。
2. **SSH heredoc 传密钥的正确姿势**：`<<'EOF'`（引号）阻断本地变量透传，远程 `$VAR` 恒为空；**密钥须经 base64 编码走命令行参数通道**（`ssh host "export B64='...'; bash -s"`），且全程只输出状态码/段长，不回显密钥。

### 10.4 ✅ stage2 收敛已执行（2026-09-26 16:02 CST）

**窗口说明（偏离原 ≥24h 门，已留痕）**：stage1 双写 05:18 生效，收敛时刻 16:02（观察 10h44m，**未跨当晚夜间流水线**）。按用户决策提前执行，以两项等价证据替代时间门：① 网关容器自 05:18 起全量日志中旧键请求 **0 条**；② Mac `YYC-Cube` 全域配置（env/sh/py/json/txt/md）grep 旧键指纹 `sk-yyc3-pro` **0 引用**。**补偿观察**：09-27 凌晨夜间流水线如出现 401/403，按 §六回滚（`.env.bak-20260926-stage2` 恢复双写）。

**操作记录**：

- 备份：`/Volume2/yyc3-33/.env.bak-20260926-stage2`（600；旧键保留至 09-27 夜间全绿后归档）
- `.env`：`API_KEYS=旧,新` → `API_KEYS=新`（51 字符 sk-17bde…）；`ADMIN_API_KEYS` 未动
- 重建：`$D compose --project-directory . -f deploy/nas/docker-compose.nas.yml up -d --no-build --force-recreate gateway`（沿用 §10.3 修正①）

**收敛验证矩阵（全绿）**：

| 项 | 本机 8000 | 公网 api.0379.world |
| -- | --------- | ------------------- |
| /healthz | **200** | — |
| 新键 `X-API-Key` | **200** | **200** |
| 旧键（收敛后应死亡） | **403** | **403** |
| 伪造键 | 403 | — |
| 无凭证 | 401 | — |

**新增实勘知识（后续轮换必读第 3 条）**：
3. 网关鉴权头是 **`X-API-Key`**；`Authorization: Bearer` 走 JWT 校验路径，对 API Key 恒返 403（"JWT token or API key is invalid"）——验证脚本一律用 `X-API-Key`。
4. NAS `containers/json?name=gateway` 会同时匹配 `0379-world-worker-1`（镜像 `yyc3-33-worker`，恰好也监听 8000 内部端口但**不注入 API_KEYS**）——定位容器必须核对 `Names=/0379-world-gateway-1`，勿用列表首元素。

**周期起算**：90 天常规轮换自 **2026-09-26** 起算 → 下次 **2026-12-25** 前（ADMIN 组错峰 +45 天）；备份文件归档条件：09-27 夜间流水线全绿。

### 10.5 部署链路追加实勘（stage2 当日发现并修复）

1. **第二重建入口**：除 cron 的 `auto-deploy.sh` 外，NAS 另有 QNAP 调度器调用的仓库根脚本 **`rebuild-gateway.sh`**（含部署后冒烟）——09-26 16:14 纯文档推送即经此脚本绕过过滤器重建一次。已给该脚本注入同款路径过滤守卫（`HEAD~1..HEAD` diff 判定）。
2. **中文路径致过滤器失效（真因）**：`git diff --name-only` 默认 `core.quotePath=true`，中文路径输出为 `"\346\223…"`（带引号+八进制转义），`^docs/` 永不匹配 → 过滤器对本仓中文文档全部失效。两脚本统一改为 **`git -c core.quotePath=false diff --name-only`**，中文文档变更实测定 SKIP。
3. **live 与仓库脚本归一**：live 根 `auto-deploy.sh` 已补齐就绪轮询（此前只入了仓库副本），三特性（路径过滤 + quotePath + 就绪轮询）双脚本齐备；两脚本均已纳入 `deploy/nas/` 版本管理。
4. **SMOKE_FAIL ≠ 部署失败**：15:23/16:15 两次 SMOKE_FAIL 中网关 DEPLOY_OK、`/healthz` 200、新键认证通过；失败项是上游旗舰路由 502（「上游降级链全部失败」，deepseek-v4-flash 正常）——属**上游 LLM 供应商侧波动**，与本次轮换/部署无关，观察供应商恢复即可。

---

## 十一、附带定案：2026-09-24 SMOKE_FAIL 根因排查（2026-09-26 结案）

**根因（实证）**：commit `b440108`（monitoring seed 清理）触发 auto-deploy，compose **build 阶段 `pip install` 层被 OOM 杀死（exit 137）**（NAS 构建内存压力）→ `COMPOSE_FAIL`；冒烟脚本随即打向 8000 端口，此时容器处于 down/重建竞态（`000000`=连接拒绝）→ 5 项全挂。**非代码回归**：5 分钟后 `e9a6b90`（OCR 上线）触发再部署成功，当前容器自 09-24 22:56:21 稳定运行、RestartCount=0、09-26 复核健康。

**防再发建议**（1/3 待办，2 已实施，3 保持现状）：

1. auto-deploy 冒烟前加 `/health` **就绪轮询**（当前是立即打断言，存在启动竞态漏报/误报）；
2. ~~NAS compose build 加内存约束或拆分 dev-deps 层~~ **✅ 已立项实施（2026-09-26，c9e2b23）**：根因实为 Dockerfile 终态是 development 致生产构建 dev 镜像——已重排 production 为默认目标 + compose 显式 `target: production` + dev 工具剥离 `requirements-dev.txt`，NAS 构建不再触碰 dev-deps 层（详见该提交）；
3. build 失败时保留旧容器运行（compose 默认行为即保留，本次冒烟失败系时序巧合，建议日志区分 BUILD_FAIL 与 SMOKE_FAIL 阶段标记——已有 COMPOSE_FAIL 标记，✅ 保持）。
